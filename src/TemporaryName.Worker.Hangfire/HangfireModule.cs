using System;
using Autofac;

namespace TemporaryName.Worker.Hangfire;

public class HangfireModule : Autofac.Module
{
    protected override void Load(ContainerBuilder builder)
    {
        base.Load(builder);

        
    }
}
